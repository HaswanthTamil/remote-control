# PAIR_TOKEN is baked into the Docker image (COPY relay-server/.env).
# Override at runtime with: docker run -e PAIR_TOKEN=...
PAIR_TOKEN=generate-a-long-random-string
PORT=3000

# Heartbeat / auth tuning (milliseconds)
# HEARTBEAT_INTERVAL=20000
# AUTH_TIMEOUT_MS=10000
# KEYS_FILE=/absolute/path/to/devices.json